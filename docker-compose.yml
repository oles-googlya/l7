---
version: '3'

services:
  dev:
    build: dev1
    volumes:
      - magictube:/tmp/magictube:z

  prod:
    image: tomcat:9.0-alpine
    ports: 
      - "8080:8080"
    volumes:
      - magictube:/usr/local/tomcat/webapps:z
    depends_on:
      - dev

volumes:
  magictube: